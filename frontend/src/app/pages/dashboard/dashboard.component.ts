import { Component, inject } from '@angular/core'; import { RouterLink } from '@angular/router'; import { AuthService } from '../../core/auth.service';
@Component({standalone:true,imports:[RouterLink],template:`<section class="hero"><div><p class="eyebrow">Área pessoal</p><h1>Olá, {{auth.user()?.name}}!</h1><p class="muted">Seus dados estão organizados e disponíveis em um só lugar.</p></div><a class="button secondary" routerLink="/perfil">Editar meus dados</a></section><section class="info"><span class="avatar">{{initial}}</span><div><strong>{{auth.user()?.name}}</strong><p class="muted">{{auth.user()?.email}}</p></div></section>`})
export class DashboardComponent{readonly auth=inject(AuthService);get initial():string{return this.auth.user()?.name?.charAt(0).toUpperCase()??'?';}}
